//Primer carruel ~UNFINISHED~

$(".slider-uno")
    .not(".slick-initialized")
    .slick({
        autoplay: true,
        autoplaySpeed: 3000,
        dots: true
    )};

//$(document).ready(function(){
   // $('.slider-uno').slick();
//});
